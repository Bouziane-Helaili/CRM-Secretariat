{% extends 'base.html.twig' %}

{% block title %}Modification{% endblock %}

{% block body %}
    <h1 class="py-5 text-center">Modification {{compagny.name}}</h1>

   {{ component('compagny_add', {
        compagny: compagny.id  ? compagny : null,
        form : form
    },  {'button_label': 'Update'}) }}
    
    

    <a href="{{ path('app_compagny_index') }}"  class="btn btn-sm btn-success">Retour à la liste</a>

    {{ include('compagny/_delete_form.html.twig') }}
{% endblock %}
