{% extends 'base.html.twig' %}

{% block title %}CategoryFile{% endblock %}

{% block body %}
    <h1>CategoryFile</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category_file.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category_file.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ category_file.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_category_file_index') }}">back to list</a>

    <a href="{{ path('app_category_file_edit', {'id': category_file.id}) }}">edit</a>

    {{ include('category_file/_delete_form.html.twig') }}
{% endblock %}
