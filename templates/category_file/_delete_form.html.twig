<form method="post" action="{{ path('app_category_file_delete', {'id': category_file.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category_file.id) }}">
    <button class="btn btn-danger">Supprimer</button>
    
</form>
