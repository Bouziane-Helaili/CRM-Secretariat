{% extends 'base.html.twig' %}

{% block title %}Liste des stagiaires
{% endblock %}

{% block body %}
	<h1 class="py-5 text-center">Liste des stagiaires</h1>

	<section class="py-5">
		<div class="container px-4 px-lg-5 mt-5">
			<div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center">
				
				{% for user in users %}
					
					<div class="col mb-5 card text-white bg-dark container p-2 ">
						<div class="card-body p-4">
							<div>
								<h5 class="fw-bolder text-center">{{ user.name }}
									{{ user.firstName }}</h5>
								<div>
									<span class="text-muted">Téléphone :
									</span>
									{{ user.phone }}</div>
								<div>
									<span class="text-muted">Email :
									</span>
									{{ user.email }}</div>
								<div>
									<span class="text-muted">Statuts :
									</span>
									{{ user.status }}</div>
							</div>
						</div>
						<div class="card-footer p-4 pt-0 border-top-0 bg-transparent">
							<div class="text-center">
								<td>
									<a href="{{ path('app_intern_show', {'id': user.id}) }}" class="btn btn-sm btn-primary">Voir la fiche</a>
									<a href="{{ path('app_intern_edit', {'id': user.id}) }}" class="btn btn-sm btn-success">Modifier</a>
								</td>
							</div>
						</div>
					</div>
	
				{% else %}
					<tr>
						<td colspan="9">Aucun stagiaire rouvé</td>
					</tr>
				{% endfor %}
			</div>
			<a href="{{ path('app_intern_new') }}" class="btn btn-sm btn-dark">Ajouter un stagiaire</a>
		</div>
	</section>
{% endblock %}
