{% extends 'base.html.twig' %}

{% block title %}Modifier un stagiaire{% endblock %}

{% block body %}
    
    <h1 class="py-5 text-center">{{user.name}} {{user.firstName}} : modifier la fiche</h1>

    {{ component('intern_add', {
        user: user.id  ? user : null,
        form : form
    }) }}

    <a href="{{ path('app_intern_index') }}" class="btn btn-success ">Revenir à la liste</a>

    {{ include('intern/_delete_form.html.twig') }}
{% endblock %}
