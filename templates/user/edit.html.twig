{% extends 'base.html.twig' %}

{% block title %}Modifier un employé{% endblock %}

{% block body %}
    <h1 class="py-5 text-center">{{user.name}} {{user.firstName}} : modifier la fiche</h1>

    {{ component('user_add', {
        user: user.id  ? user : null,
        form : form
    }) }}

    <a href="{{ path('app_user_index') }}" class="btn btn-success ">Revenir à la liste</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
